package com.learn.day02;/*1.java 定义格式：   数据类型 变量名 = 变量值;2.说明：    变量必须先声明和初始化再使用    变量都定义在作用域内，作用域之外就失效    同一个作用域内，不用使用同名3.数据类型——基本数据类型：    整数：byte（8位/1字节 -128~127 分正负 2的7次方），short（2字节），int（4字节），long（8字节）    浮点类型：float（4字节 精确度7位有效数字），double（8字节） 默认double    字符型：char(不是字符串) 2字节 必须使用单引号    布尔型：boolean4.引用数据类型：    类：class（包含字符串）    接口：interface    数组：[] array5.变量在类中的位置 成员变量 vs 局部变量 */public class Variable {    public static void main(String[] args) {        //byte（8位/1字节 -128~127 分正负 2的7次方），short（2字节），int（4字节），long（8字节）        byte b1 = 1;        //byte b2 = 128; 会超过范围        System.out.println(b1);        //声明long变量必须以l或L结尾        short s1 = 128;        int i1 = 1234;        long l1 = 143242353454L;        //浮点类型：float（4字节 精确度7位有效数字），double（8字节）        //默认double 声明float需要以f或F结尾        //float数值范围比long还大        double d1 = 123.3;        float f1 = 12.3f;        System.out.println(d1 + 1);        System.out.println(f1);        //字符型：char（2字节） 必须使用单引号        char c1 = 'a';        char c2 = '中';        //char c1 = 'ab'; 不行 只能写一个        System.out.println(c1);        System.out.println(c2);        //转义字符        // \n换行 \t制表符（tap） \r 回车 \b退格 \"双引号 \'单引号 \\ 反斜线        char c3 = '\r';        System.out.print("hello" + c3);        System.out.println("world");        char c4 = '\t'; //制表符 类似于tap键        System.out.print("hello" + c4);        System.out.println("world");        // 使用Unicode字符(utf-8)        char c5 = '\u0123';        System.out.println(c5);        //布尔型：boolean        boolean bb1 = true;        System.out.println(bb1);        boolean isMarried = true;        if (isMarried){            System.out.println("不行");        }else {            System.out.println("可以");        }    }    public void method() {        int myClass = 1;// 只能用于当前作用域    }}